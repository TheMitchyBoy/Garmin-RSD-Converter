services:
  sonar-web:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - sonar-data:/data
    environment:
      SONAR_HOST: "0.0.0.0"
      SONAR_PORT: "8080"
      SONAR_DATA_DIR: "/data"
      SONAR_MAX_UPLOAD_BYTES: "2147483648"

volumes:
  sonar-data:
